import React, { createContext, useState } from 'react'

export const UseContext = createContext();

export const useProvider = ({children}) => {
    const [user, setUser] = useState({name:"varsha", age:20})
  return (
   User
  )
}

export default UseContext
